# Development install: pip install -e ".[dev]"
# Models download automatically on first run (cached in ~/.cache/visiondrop).
mediapipe>=0.10.20
numpy>=1.24
opencv-python>=4.8
sounddevice>=0.5
ultralytics>=8.3
lap>=0.5.12
